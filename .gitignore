node_modules/
dist/
test-build/
*.tgz

# default and conventional extraction targets, never part of the repository
out/
extracted/

# local scratch, not for the repository
*.local.md

# scratch space for tests, never part of the repository
.tmp/
